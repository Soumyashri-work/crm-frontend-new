/**
 * Common pagination constants used across all pages
 */

export const PAGE_SIZE = {
  SMALL: 10,   // Used by SuperAdminTenants, SuperAdminAdmins, etc
  DEFAULT: 20, // Used by Customers, Agents, etc
};

export const DEFAULT_PAGE_SIZE = PAGE_SIZE.DEFAULT;